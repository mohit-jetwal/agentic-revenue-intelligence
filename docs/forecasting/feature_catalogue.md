# Forecasting feature catalogue

> Generated by `docs/generate_feature_catalogue.py`. Do not edit by hand.

Every feature the forecasting model can see, with **when its value becomes knowable** and **what leakage risk it carries**. The risk column is derived from the feature's declared temporality plus the forecaster's own exclusion lists - it is not a separate hand-maintained judgement, because a duplicated fact drifts.

## How to read the columns

| Column | Meaning |
|---|---|
| **available at** | `origin t` - knowable when the forecast is made. `target date t+h` - the day being forecast, legitimate only for `KNOWN_IN_ADVANCE` tables. |
| **risk** | `none` safe by construction; `low` same-date but origin-only; `managed` reads forward with a written justification; `EXCLUDED` never reaches the model. |

## Why some features are excluded outright

| Exclusion | Reason |
|---|---|
| target-derived (`revenue`, `cost`, `gross_profit`) | `revenue = units x price` recovers the target exactly |
| supply (`inventory_*`, `stockout_*`) | Step 4 measured it: with inventory features the model recovered only 0.30 of true stockout demand, having learned that low stock predicts low sales |
| `time_index` | anchored to the frame's own minimum, so the same calendar date differs between training and serving |
| `year`, `financial_year` | either a year the model has seen and overfits, or one it cannot place |
| `part` | hive partition key - a storage artifact that does not exist for a future date |

## competitor

| feature | definition | available at | risk | why |
|---|---|---|---|---|
| `competitor_discount` | Mean competitor discount depth. (mean by (date, product)) | origin `t` | **none** | computed from history strictly before the row's date |
| `competitor_price` | Mean effective competitor price for this product on this date. (mean by (date, product)) | origin `t` | **none** | computed from history strictly before the row's date |
| `competitor_price_index` | competitor_price / own_price. (competitor / own) | origin `t` | **none** | computed from history strictly before the row's date |
| `competitor_promotion_flag` | Any competitor promoting this product on this date. (max by (date, product)) | origin `t` | **none** | computed from history strictly before the row's date |
| `price_gap` | own_price - competitor_price, in currency. (own - competitor) | origin `t` | **none** | computed from history strictly before the row's date |
| `price_ratio` | own_price / competitor_price. Scale-free; linear in a log model. (own / competitor) | origin `t` | **none** | computed from history strictly before the row's date |

## demand

| feature | definition | available at | risk | why |
|---|---|---|---|---|
| `demand_momentum` | 7-day mean over 28-day mean. Above 1 means accelerating. (rolling_7 / rolling_28, both shifted) | origin `t` | **none** | computed from history strictly before the row's date |
| `demand_trend_28` | Growth of the recent week against four weeks ago. ((rolling_7 - lag_28) / lag_28) | origin `t` | **none** | computed from history strictly before the row's date |
| `demand_volatility` | Coefficient of variation over 28 days; widens forecast intervals. (rolling_28_std / rolling_28_mean, both shifted) | origin `t` | **none** | computed from history strictly before the row's date |
| `lag_14_units` | Units sold 14 day(s) earlier for this product-store. (groupby(product,store).shift(14)) | origin `t` | **none** | computed from history strictly before the row's date |
| `lag_1_units` | Units sold 1 day(s) earlier for this product-store. (groupby(product,store).shift(1)) | origin `t` | **none** | computed from history strictly before the row's date |
| `lag_28_units` | Units sold 28 day(s) earlier for this product-store. (groupby(product,store).shift(28)) | origin `t` | **none** | computed from history strictly before the row's date |
| `lag_364_units` | Units sold 364 day(s) earlier for this product-store. (groupby(product,store).shift(364)) | origin `t` | **none** | computed from history strictly before the row's date |
| `lag_56_units` | Units sold 56 day(s) earlier for this product-store. (groupby(product,store).shift(56)) | origin `t` | **none** | computed from history strictly before the row's date |
| `lag_7_units` | Units sold 7 day(s) earlier for this product-store. (groupby(product,store).shift(7)) | origin `t` | **none** | computed from history strictly before the row's date |
| `rolling_14_units` | Mean units over the 14 days ending yesterday. Excludes the current day by construction. (shift(1).rolling(14).mean()) | origin `t` | **none** | computed from history strictly before the row's date |
| `rolling_14_units_std` | Volatility of units over the 14 days ending yesterday. (shift(1).rolling(14).std()) | origin `t` | **none** | computed from history strictly before the row's date |
| `rolling_28_units` | Mean units over the 28 days ending yesterday. Excludes the current day by construction. (shift(1).rolling(28).mean()) | origin `t` | **none** | computed from history strictly before the row's date |
| `rolling_28_units_std` | Volatility of units over the 28 days ending yesterday. (shift(1).rolling(28).std()) | origin `t` | **none** | computed from history strictly before the row's date |
| `rolling_56_units` | Mean units over the 56 days ending yesterday. Excludes the current day by construction. (shift(1).rolling(56).mean()) | origin `t` | **none** | computed from history strictly before the row's date |
| `rolling_56_units_std` | Volatility of units over the 56 days ending yesterday. (shift(1).rolling(56).std()) | origin `t` | **none** | computed from history strictly before the row's date |
| `rolling_7_units` | Mean units over the 7 days ending yesterday. Excludes the current day by construction. (shift(1).rolling(7).mean()) | origin `t` | **none** | computed from history strictly before the row's date |
| `rolling_7_units_std` | Volatility of units over the 7 days ending yesterday. (shift(1).rolling(7).std()) | origin `t` | **none** | computed from history strictly before the row's date |

## inventory

| feature | definition | available at | risk | why |
|---|---|---|---|---|
| `days_since_stockout` | Days since the last stockout, counting prior days only. (days since last true, shifted by 1) | origin `t` | **EXCLUDED** | supply signal - the model would learn censoring as demand |
| `inventory_available` | Opening stock plus deliveries; known before any sale. (opening + received) | origin `t` | **EXCLUDED** | supply signal - the model would learn censoring as demand |
| `inventory_days_cover` | Days of cover at trailing demand rate. (inventory_available / shifted rolling_28 demand) | origin `t` | **EXCLUDED** | supply signal - the model would learn censoring as demand |
| `inventory_ratio` | Availability over trailing 28-day mean demand. (inventory_available / shifted rolling_28 demand) | origin `t` | **EXCLUDED** | supply signal - the model would learn censoring as demand |
| `stockout_yesterday` | Whether yesterday was a stockout. Today's is not knowable. (shift(1)) | origin `t` | **EXCLUDED** | supply signal - the model would learn censoring as demand |
| `stockouts_last_28d` | Stockout days in the 28 days ending yesterday. (shift(1).rolling(28).sum()) | origin `t` | **EXCLUDED** | supply signal - the model would learn censoring as demand |
| `stockouts_last_90d` | Stockout days in the 90 days ending yesterday. (shift(1).rolling(90).sum()) | origin `t` | **EXCLUDED** | supply signal - the model would learn censoring as demand |

## price

| feature | definition | available at | risk | why |
|---|---|---|---|---|
| `discount_depth` | 1 - selling/regular. Recomputed rather than trusted. (1 - selling_price / regular_price) | origin `t` | **low** | same-date value, read at the origin only |
| `historical_average_price` | Expanding mean of price over all prior days. (shift(1).expanding().mean()) | origin `t` | **none** | computed from history strictly before the row's date |
| `price_change_pct_1` | Price change against yesterday. ((price - shift(1)) / shift(1)) | origin `t` | **low** | same-date value, read at the origin only |
| `price_change_pct_7` | Price change against a week ago. ((price - shift(7)) / shift(7)) | origin `t` | **low** | same-date value, read at the origin only |
| `price_index` | Own price over the same-day category mean. Above 1 means priced above the competitive set on the shelf. (selling_price / mean(selling_price) by (date, category)) | origin `t` | **low** | same-date value, read at the origin only |
| `price_vs_rolling_average` | Today's price over this item's own trailing 28-day mean. (price / shift(1).rolling(28).mean()) | origin `t` | **low** | same-date value, read at the origin only |
| `regular_price` | Undiscounted shelf price. (passthrough) | origin `t` | **low** | same-date value, read at the origin only |
| `selling_price` | Price actually paid after any promotional discount. (passthrough) | origin `t` | **low** | same-date value, read at the origin only |

## product

| feature | definition | available at | risk | why |
|---|---|---|---|---|
| `brand` | Brand. (joined; categorical dtype, not one-hot) | origin `t` | **low** | same-date value, read at the origin only |
| `category` | Product category. (joined; categorical dtype, not one-hot) | origin `t` | **low** | same-date value, read at the origin only |
| `is_new_product` | Within 90 days of launch; distribution still building. (product_age_days < 90) | origin `t` | **low** | same-date value, read at the origin only |
| `pack_size` | Pack size label. (joined; categorical dtype, not one-hot) | origin `t` | **low** | same-date value, read at the origin only |
| `product_age_days` | Days since launch, relative to the row's own date. (row date - launch_date) | origin `t` | **low** | same-date value, read at the origin only |
| `product_status` | Active / Launched / Discontinued. (joined; categorical dtype, not one-hot) | origin `t` | **low** | same-date value, read at the origin only |
| `subcategory` | Product subcategory. (joined; categorical dtype, not one-hot) | origin `t` | **low** | same-date value, read at the origin only |
| `unit_cost` | Cost per unit; needed for margin and optimisation. (joined) | origin `t` | **low** | same-date value, read at the origin only |

## promotion

| feature | definition | available at | risk | why |
|---|---|---|---|---|
| `days_into_promotion` | Days elapsed since this promotion started. (date - start_date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `days_since_promotion` | Days since the previous promotion ended. (days since last true, shifted by 1) | target date `t+h` | **none** | computed from history strictly before the row's date |
| `days_to_next_promotion` | Days until the next scheduled promotion begins. (merge_asof forward on start_date) | target date (planned) | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `days_until_promotion_end` | Days remaining in the active promotion. (end_date - date) | target date (planned) | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `promotion_discount` | Depth of the active promotion, as a fraction. (discount_percentage / 100) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `promotion_duration` | Total length of the active promotion in days. (passthrough from the event) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `promotion_flag` | A promotion is active on this product-store-date. (expanded promotion calendar, joined) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `promotion_intensity` | Spend per day of the event, so long and short promos compare. (promotion_spend / promotion_duration) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `promotion_spend` | Realised spend on the active promotion. Historical analysis only - masked beyond the as-of date because it is an actual. (passthrough from the event) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `promotions_last_28d` | Promoted days in the 28 days ending yesterday. (shift(1).rolling(28).sum()) | target date `t+h` | **none** | computed from history strictly before the row's date |
| `promotions_last_90d` | Promoted days in the 90 days ending yesterday. (shift(1).rolling(90).sum()) | target date `t+h` | **none** | computed from history strictly before the row's date |

## store

| feature | definition | available at | risk | why |
|---|---|---|---|---|
| `channel` | Hypermarket, Supermarket, Convenience, E-commerce, Wholesale. (joined; categorical dtype, not one-hot) | origin `t` | **low** | same-date value, read at the origin only |
| `region` | Region. (joined; categorical dtype, not one-hot) | origin `t` | **low** | same-date value, read at the origin only |
| `state` | State. (joined; categorical dtype, not one-hot) | origin `t` | **EXCLUDED** | target-derived or non-reproducible at serve time |
| `store_age_days` | Days since opening, relative to the row's own date. (row date - opening_date) | origin `t` | **low** | same-date value, read at the origin only |
| `store_size_sqft` | Floor area; 0 for e-commerce. (joined) | origin `t` | **low** | same-date value, read at the origin only |
| `store_type` | Flagship or Standard. (joined; categorical dtype, not one-hot) | origin `t` | **low** | same-date value, read at the origin only |

## temporal

| feature | definition | available at | risk | why |
|---|---|---|---|---|
| `day_of_week` | Monday = 0. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `days_since_festival` | Days since the most recent festival. (searchsorted over festival dates) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `days_to_festival` | Days until the next festival. (searchsorted over festival dates) | target date (planned) | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `dow_cos` | Cyclical encoding of day of week. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `dow_sin` | Cyclical encoding of day of week. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `doy_cos` | Cyclical encoding of day of year. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `doy_sin` | Cyclical encoding of day of year. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `festival_flag` | Festival window including the run-up. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `financial_month` | Indian financial year, April = 1. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `financial_quarter` | Financial quarter. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `holiday_flag` | Public holiday. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `month` | Calendar month. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `month_cos` | Cyclical encoding of month. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `month_sin` | Cyclical encoding of month. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `quarter` | Calendar quarter. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `season` | Season label. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `time_index` | Days since the window start; linear trend term. (derived from the date) | target date `t+h` | **EXCLUDED** | target-derived or non-reproducible at serve time |
| `week_of_year` | ISO week number. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `weekend_flag` | Saturday or Sunday. (derived from the date) | target date `t+h` | **none** | origin-side copy dropped; the `h_` target-date copy is used |
| `year` | Calendar year. (derived from the date) | target date `t+h` | **EXCLUDED** | target-derived or non-reproducible at serve time |

## Never features

Columns present in the panel that are removed before the model sees anything:

```
  closing_inventory_lag_1
  date
  days_since_stockout
  financial_year
  inventory_available
  inventory_days_cover
  inventory_days_lag_1
  inventory_ratio
  opening_inventory
  origin_date
  part
  price_change_reason
  promotion_id
  received_units
  sold_units_lag_1
  stockout_flag
  stockout_yesterday
  stockouts_last_28d
  stockouts_last_90d
  target_date
  time_index
  units
  units_uncensored
  year
```

## Verification

These are not documentation-only claims. `tests/forecasting/test_dataset.py` asserts that no supply feature, no target-derived column, no `time_index` and no hive partition key reaches the feature set, and `tests/forecasting/test_leakage.py` corrupts all `OBSERVED` data after a cutoff and asserts the training features are byte-identical.
